{
  "name": "vibe-prompt-tool",
  "private": true,
  "description": "MotionSites-style vibe prompt generator (static)",
  "scripts": {
    "build": "node build-catalog.mjs",
    "fetch:motion": "node fetch-reactbits.mjs && node fetch-motion-docs.mjs && node build-catalog.mjs"
  },
  "engines": {
    "node": "20.x"
  }
}
